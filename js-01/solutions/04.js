const waitingLine = ["Alice", "Mateo", "Priya", "Jonas", "Sam"];

// Classic for-loop approach
for (let i = 0; i < waitingLine.length; i++) {
  console.log(`Next in line: ${waitingLine[i]}, step right up!`);
}