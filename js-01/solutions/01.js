// Define the variables
const customerName = "Sarah";
let orderNumber = 1;
let isIced = false;
const drinkName = "Cappuccino";

// Log the result using template literals (or string concatenation)
console.log(`Order #${orderNumber}: ${customerName} wants a hot ${drinkName}.`);

// Alternatively, using older string concatenation if you haven't taught template literals yet:
// console.log("Order #" + orderNumber + ": " + customerName + " wants a hot " + drinkName + ".");
